const v1 = prompt('first number');
const a = Number(v1);
const v2 = prompt('second number');
const b = Number(v2);

const result = `${a} + ${b} = ${a + b}
${a} - ${b} = ${a - b}
${a} * ${b} = ${a * b}
${a} / ${b} = ${a / b}
`
alert(result)
